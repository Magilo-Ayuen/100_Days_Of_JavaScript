// Every standard function needs a declaration, parameters (inputs), and a return value (output).

//declaration - functions are declared using function keyword
//a and b are the parameters or inputs
function add (a,b) {
    return a + b;
}
//calling the add function with input parameters
console.log(add(34,67));