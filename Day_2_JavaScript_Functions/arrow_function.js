//With Multiple parameters

const mul = (x,y) => x * y;
console.log(mul(34,56));

// With a single parameter

const mul2 = (x) => x * 2; // you can omit parentheses
console.log(mul2(99));

// With no parameters

const hello = () => "Hello! how are you ?";
console.log(hello());